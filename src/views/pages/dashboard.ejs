<div class="space-y-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-2xl font-extrabold tracking-tight mb-1 text-secondary">Operational Overview</h1>
        <p class="text-sm text-muted">Real-time revenue monitoring and activity.</p>
      </div>
      <a href="/invoices/new" class="bg-primary text-white px-5 py-2 rounded-md text-xs font-bold flex items-center gap-2 hover:bg-secondary transition-all">
        <i data-lucide="plus" class="w-4 h-4"></i> New Invoice
      </a>
    </div>

    <!-- Main Stats Grid -->
    <div class="grid md:grid-cols-4 gap-4">
        <div class="bg-white p-6 rounded-[16px] border border-border shadow-sm">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center mb-4 bg-blue-50">
                <i data-lucide="trending-up" class="w-5 h-5 text-primary"></i>
            </div>
            <div class="text-[10px] font-bold uppercase tracking-widest text-muted mb-1">Total Revenue</div>
            <div class="text-xl font-extrabold font-mono tracking-tight text-secondary">$14,200.00</div>
        </div>
        <div class="bg-white p-6 rounded-[16px] border border-border shadow-sm">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center mb-4 bg-emerald-50">
                <i data-lucide="check-circle-2" class="w-5 h-5 text-emerald-600"></i>
            </div>
            <div class="text-[10px] font-bold uppercase tracking-widest text-muted mb-1">Paid</div>
            <div class="text-xl font-extrabold font-mono tracking-tight text-secondary">$9,850.00</div>
        </div>
        <div class="bg-white p-6 rounded-[16px] border border-border shadow-sm">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center mb-4 bg-amber-50">
                <i data-lucide="clock" class="w-5 h-5 text-amber-600"></i>
            </div>
            <div class="text-[10px] font-bold uppercase tracking-widest text-muted mb-1">Pending</div>
            <div class="text-xl font-extrabold font-mono tracking-tight text-secondary">$3,200.00</div>
        </div>
        <div class="bg-white p-6 rounded-[16px] border border-border shadow-sm">
            <div class="w-10 h-10 rounded-lg flex items-center justify-center mb-4 bg-rose-50">
                <i data-lucide="alert-circle" class="w-5 h-5 text-rose-600"></i>
            </div>
            <div class="text-[10px] font-bold uppercase tracking-widest text-muted mb-1">Overdue</div>
            <div class="text-xl font-extrabold font-mono tracking-tight text-secondary">$1,150.00</div>
        </div>
    </div>

    <div class="grid lg:grid-cols-3 gap-6">
      <!-- Chart Card (Placeholder) -->
      <div class="lg:col-span-2 bg-white rounded-[16px] p-8 border border-border shadow-sm relative overflow-hidden">
         <div class="flex items-center justify-between mb-8">
           <h3 class="font-bold text-sm text-secondary uppercase tracking-tight">Revenue Distribution</h3>
           <div class="text-[10px] font-bold text-primary italic uppercase tracking-widest">Live Feed</div>
         </div>
         
         <div class="h-[280px] w-full flex items-end gap-2 px-4">
             <!-- Placeholder chart using divs -->
             <div class="flex-1 bg-primary rounded-t-lg" style="height: 60%"></div>
             <div class="flex-1 bg-primary/60 rounded-t-lg" style="height: 80%"></div>
             <div class="flex-1 bg-primary rounded-t-lg" style="height: 40%"></div>
             <div class="flex-1 bg-primary/60 rounded-t-lg" style="height: 90%"></div>
             <div class="flex-1 bg-primary rounded-t-lg" style="height: 50%"></div>
         </div>
         <div class="flex justify-between mt-4 text-[10px] font-bold text-muted uppercase">
             <span>Mon</span><span>Tue</span><span>Wed</span><span>Thu</span><span>Fri</span>
         </div>
      </div>

      <!-- Recent Activity Card -->
      <div class="bg-white rounded-[16px] p-8 border border-border shadow-sm flex flex-col">
          <h3 class="font-bold text-sm text-secondary uppercase tracking-tight mb-6">Recent Activity</h3>
          <div class="space-y-5 flex-1">
            <% if (invoices.length === 0) { %>
               <div class="py-10 text-center space-y-4">
                 <div class="w-14 h-14 bg-slate-50 rounded-full flex items-center justify-center mx-auto text-slate-300">
                   <i data-lucide="file-text" class="w-7 h-7"></i>
                 </div>
                 <p class="text-[10px] font-bold text-muted uppercase tracking-widest">Quiet in the workspace</p>
               </div>
            <% } else { %>
              <% invoices.forEach(inv => { %>
                <div class="flex items-start justify-between group cursor-pointer transition-all hover:translate-x-1">
                    <div class="flex gap-3">
                      <div class="w-9 h-9 rounded-lg flex items-center justify-center bg-slate-50 text-slate-400">
                        <i data-lucide="file-text" class="w-4 h-4"></i>
                      </div>
                      <div>
                        <div class="font-bold text-xs text-secondary"><%= inv.invoiceNumber %></div>
                        <div class="text-[9px] text-muted font-bold uppercase tracking-tight">2 minutes ago</div>
                      </div>
                    </div>
                    <div class="text-right">
                       <div class="font-bold text-xs tracking-tight text-secondary">$<%= inv.total %></div>
                       <div class="text-[8px] font-extrabold uppercase tracking-widest text-slate-400">
                         <%= inv.status %>
                       </div>
                    </div>
                  </div>
              <% }) %>
            <% } %>
          </div>

          <a href="/invoices" class="mt-6 flex items-center justify-center gap-2 text-[9px] font-bold uppercase tracking-widest text-primary hover:text-secondary transition-colors pt-4 border-t border-slate-50 w-full">
            View Ledger <i data-lucide="arrow-up-right" class="w-3.5 h-3.5"></i>
          </a>
      </div>
    </div>
</div>
